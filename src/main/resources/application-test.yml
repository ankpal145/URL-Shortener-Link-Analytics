app:
  base-url: http://localhost
  ip-hash-salt: test-salt
  short-code:
    length: 7
    max-collision-retries: 5

spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=LEGACY
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
